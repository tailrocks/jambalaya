mutation tableCreate($input: TableCreateInput!) {
  tableCreate(input: $input, additionalArgument2: "xyz") {
    data {
      name
      columns {
        ... tableColumnFragment
      }
    }
    error {
      ... on SecurityError {
        code
        message
      }
    }
  }
}
