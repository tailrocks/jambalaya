# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / Main
run-name: CI / main · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      runner:
        description: Execution backend
        required: false
        default: both
        type: choice
        options:
          - velnor
          - github
          - both
      scope:
        description: Verification scope
        required: true
        default: full
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      full_units: ${{ steps.plan.outputs.full_units }}
      gradle_matrix: ${{ steps.plan.outputs.gradle_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        if: ${{ runner.environment == 'github-hosted' }}
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@5e311cace1fdfc1b58bf2f620c93cd815a9f005c
        with:
          rev: 5e311cace1fdfc1b58bf2f620c93cd815a9f005c
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=5e311cace1fdfc1b58bf2f620c93cd815a9f005c" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_LANES: ${{ github.event.inputs.runner || 'both' }}
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          velnor-workflow plan --config .github/ci/project.toml

      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 5e311cace1fdfc1b58bf2f620c93cd815a9f005c
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          [[ "$policy_revision" == "$EXPECTED_REVISION" ]] || { echo "::error::policy runtime reports revision $policy_revision, expected $EXPECTED_REVISION" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-5e311cace1fdfc1b58bf2f620c93cd815a9f005c-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  velnor-lane-admission:
    name: "Control / Velnor admission"
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Velnor lane omitted for fork
        run: |
          echo '::notice::Velnor lane omitted for fork pull request; validating GitHub lane only.'
  policy:
    name: Policy
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    # Trust invariant: this job runs the base branch's validator against the
    # audited tree under pull_request_target. It holds `contents: read` only,
    # references no secrets, and its checkout persists no credentials, so
    # building and running the tree's declared generator here is no more
    # privileged than the pull_request lanes that already build the tree.
    permissions:
      contents: read
    steps:
      - name: Checkout repository history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: policy-checkout
          fetch-depth: 0
          persist-credentials: false
      - name: Check out audited head
        working-directory: policy-checkout
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
        run: |
          set -euo pipefail
          if ! git cat-file -e "$HEAD_SHA^{commit}" 2>/dev/null; then
            git fetch --no-tags "$GITHUB_SERVER_URL/$HEAD_REPOSITORY" "$HEAD_SHA"
          fi
          git checkout --quiet --detach "$HEAD_SHA"
      - name: Bound the Mr. Boxington store
        shell: bash
        run: echo "MBX_GC_MAX_SIZE=12GiB" >> "$GITHUB_ENV"
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          version: 1.11.1
          cache-key: velnor-policy-mbx-1.11.1-${{ runner.os }}-${{ runner.arch }}-5e311cace1fdfc1b58bf2f620c93cd815a9f005c
          restore-keys: |
            velnor-policy-mbx-1.11.1-${{ runner.os }}-${{ runner.arch }}-
      - name: Install pinned Velnor workflow runtime
        env:
          CARGO_HOME: ${{ runner.temp }}/velnor-workflow-cargo-home
          CARGO_TARGET_DIR: ${{ runner.temp }}/velnor-workflow-cargo-target
          VELNOR_WORKFLOW_INSTALL_DIR: ${{ runner.temp }}/velnor-workflow-install
          VELNOR_WORKFLOW_ROOT: ${{ runner.temp }}/velnor-workflow
        run: |
          set -euo pipefail
          install -d -m 700 \
            "$CARGO_HOME" \
            "$CARGO_TARGET_DIR" \
            "$VELNOR_WORKFLOW_INSTALL_DIR"
          cd "$VELNOR_WORKFLOW_INSTALL_DIR"
          env -u RUSTC_WRAPPER -u SCCACHE_GHA_ENABLED -u CARGO_INCREMENTAL -u RUSTFLAGS -u CARGO_ENCODED_RUSTFLAGS \
            cargo install \
            --locked \
            --git https://github.com/tailrocks/velnor \
            --rev 5e311cace1fdfc1b58bf2f620c93cd815a9f005c \
            --root "$VELNOR_WORKFLOW_ROOT" \
            velnor-workflow \
            --bin velnor-workflow
          echo "$VELNOR_WORKFLOW_ROOT/bin" >> "$GITHUB_PATH"
      - name: Resolve required status checks
        env:
          GH_TOKEN: ${{ github.token }}
          DEFAULT_BRANCH: main
        run: |
          set -euo pipefail
          contexts="$(gh api "repos/$GITHUB_REPOSITORY/rulesets?includes_parents=true" \
            | jq -r '.[] | select(.target == "branch" and .enforcement == "active") | .id' \
            | while read -r id; do gh api "repos/$GITHUB_REPOSITORY/rulesets/$id"; done \
            | jq -r --arg branch "refs/heads/$DEFAULT_BRANCH" 'select(.conditions.ref_name.include | any(. == "~DEFAULT_BRANCH" or . == "~ALL" or . == $branch)) | .rules[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context' \
            | sort -u | paste -sd, -)"
          echo "RULESET_CONTEXTS=$contexts" >> "$GITHUB_ENV"
      - name: Enforce workflow policy
        env:
          WORKFLOW_ROOT: ${{ github.workspace }}/policy-checkout
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
          VELNOR_WORKFLOW_POLICY_REVISION: 5e311cace1fdfc1b58bf2f620c93cd815a9f005c
        run: |
          set -euo pipefail
          velnor-workflow policy \
            --workflow-root "$WORKFLOW_ROOT" \
            --head-sha "$HEAD_SHA" \
            --base-sha "$BASE_SHA" \
            --ruleset-contexts "$RULESET_CONTEXTS"
      - name: Set up actionlint
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: actionlint@1.7.12
          cache: false
      - name: Lint caller workflows
        working-directory: policy-checkout
        run: mise exec actionlint@1.7.12 -- actionlint
  github-gradle:
    name: "Gradle · Gradle"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle:
    name: "Gradle · Gradle"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-buildsrc:
    name: "Gradle · Gradle (buildSrc)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-buildsrc,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-buildsrc
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        buildSrc/gradle/wrapper/gradle-wrapper.properties
        buildSrc/gradle/libs.versions.toml
  velnor-gradle-buildsrc:
    name: "Gradle · Gradle (buildSrc)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-buildsrc,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-buildsrc
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        buildSrc/gradle/wrapper/gradle-wrapper.properties
        buildSrc/gradle/libs.versions.toml
  github-gradle-jambalaya-checks:
    name: "Gradle · Gradle (jambalaya-checks)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-checks,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-checks
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-checks:
    name: "Gradle · Gradle (jambalaya-checks)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-checks,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-checks
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-checks-jooq:
    name: "Gradle · Gradle (jambalaya-checks-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-checks-jooq,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-checks-jooq
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-checks-jooq:
    name: "Gradle · Gradle (jambalaya-checks-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-checks-jooq,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-checks-jooq
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-example-grpc-interface:
    name: "Gradle · Gradle (jambalaya-example-grpc-interface)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-grpc-interface,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-grpc-interface
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-example-grpc-interface:
    name: "Gradle · Gradle (jambalaya-example-grpc-interface)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-grpc-interface,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-grpc-interface
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-example-jooq:
    name: "Gradle · Gradle (jambalaya-example-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-jooq,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-jooq
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-example-jooq:
    name: "Gradle · Gradle (jambalaya-example-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-jooq,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-jooq
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-example-kotlin-test-graphql:
    name: "Gradle · Gradle (jambalaya-example-kotlin-test-graphql)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-kotlin-test-graphql,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-kotlin-test-graphql
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-example-kotlin-test-graphql:
    name: "Gradle · Gradle (jambalaya-example-kotlin-test-graphql)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-kotlin-test-graphql,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-kotlin-test-graphql
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-example-mapstruct-processors:
    name: "Gradle · Gradle (jambalaya-example-mapstruct-processors)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-mapstruct-processors,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-mapstruct-processors
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-example-mapstruct-processors:
    name: "Gradle · Gradle (jambalaya-example-mapstruct-processors)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-example-mapstruct-processors,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-example-mapstruct-processors
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-graphql:
    name: "Gradle · Gradle (jambalaya-graphql)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-graphql,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-graphql
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-graphql:
    name: "Gradle · Gradle (jambalaya-graphql)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-graphql,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-graphql
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-graphql-apollo:
    name: "Gradle · Gradle (jambalaya-graphql-apollo)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-graphql-apollo,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-graphql-apollo
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-graphql-apollo:
    name: "Gradle · Gradle (jambalaya-graphql-apollo)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-graphql-apollo,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-graphql-apollo
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-graphql-jooq:
    name: "Gradle · Gradle (jambalaya-graphql-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-graphql-jooq,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-graphql-jooq
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-graphql-jooq:
    name: "Gradle · Gradle (jambalaya-graphql-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-graphql-jooq,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-graphql-jooq
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-jsr310:
    name: "Gradle · Gradle (jambalaya-jsr310)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-jsr310,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-jsr310
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-jsr310:
    name: "Gradle · Gradle (jambalaya-jsr310)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-jsr310,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-jsr310
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-junit-opentelemetry:
    name: "Gradle · Gradle (jambalaya-junit-opentelemetry)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-junit-opentelemetry,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-junit-opentelemetry
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-junit-opentelemetry:
    name: "Gradle · Gradle (jambalaya-junit-opentelemetry)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-junit-opentelemetry,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-junit-opentelemetry
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-kotlin-test:
    name: "Gradle · Gradle (jambalaya-kotlin-test)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-kotlin-test,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-kotlin-test
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        jambalaya-kotlin-test/gradle/wrapper/gradle-wrapper.properties
        jambalaya-kotlin-test/gradle/libs.versions.toml
  velnor-gradle-jambalaya-kotlin-test:
    name: "Gradle · Gradle (jambalaya-kotlin-test)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-kotlin-test,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-kotlin-test
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        jambalaya-kotlin-test/gradle/wrapper/gradle-wrapper.properties
        jambalaya-kotlin-test/gradle/libs.versions.toml
  github-gradle-jambalaya-mapstruct:
    name: "Gradle · Gradle (jambalaya-mapstruct)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-mapstruct,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-mapstruct
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-mapstruct:
    name: "Gradle · Gradle (jambalaya-mapstruct)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-mapstruct,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-mapstruct
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-mapstruct-processor:
    name: "Gradle · Gradle (jambalaya-mapstruct-processor)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-mapstruct-processor,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-mapstruct-processor
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-mapstruct-processor:
    name: "Gradle · Gradle (jambalaya-mapstruct-processor)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-mapstruct-processor,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-mapstruct-processor
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-micronaut-graphql:
    name: "Gradle · Gradle (jambalaya-micronaut-graphql)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-micronaut-graphql,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-micronaut-graphql
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-micronaut-graphql:
    name: "Gradle · Gradle (jambalaya-micronaut-graphql)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-micronaut-graphql,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-micronaut-graphql
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-micronaut-mapstruct-protobuf:
    name: "Gradle · Gradle (jambalaya-micronaut-mapstruct-protobuf)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-micronaut-mapstruct-protobuf,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-micronaut-mapstruct-protobuf
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-micronaut-mapstruct-protobuf:
    name: "Gradle · Gradle (jambalaya-micronaut-mapstruct-protobuf)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-micronaut-mapstruct-protobuf,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-micronaut-mapstruct-protobuf
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-opentelemetry:
    name: "Gradle · Gradle (jambalaya-opentelemetry)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-opentelemetry,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-opentelemetry
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-opentelemetry:
    name: "Gradle · Gradle (jambalaya-opentelemetry)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-opentelemetry,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-opentelemetry
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-protobuf:
    name: "Gradle · Gradle (jambalaya-protobuf)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-protobuf,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-protobuf
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-protobuf:
    name: "Gradle · Gradle (jambalaya-protobuf)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-protobuf,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-protobuf
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-seo:
    name: "Gradle · Gradle (jambalaya-seo)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-seo,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-seo
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-seo:
    name: "Gradle · Gradle (jambalaya-seo)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-seo,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-seo
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-tenancy:
    name: "Gradle · Gradle (jambalaya-tenancy)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-tenancy:
    name: "Gradle · Gradle (jambalaya-tenancy)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-tenancy-flyway:
    name: "Gradle · Gradle (jambalaya-tenancy-flyway)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-flyway,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-flyway
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-tenancy-flyway:
    name: "Gradle · Gradle (jambalaya-tenancy-flyway)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-flyway,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-flyway
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-tenancy-grpc-interface:
    name: "Gradle · Gradle (jambalaya-tenancy-grpc-interface)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-grpc-interface,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-grpc-interface
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-tenancy-grpc-interface:
    name: "Gradle · Gradle (jambalaya-tenancy-grpc-interface)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-grpc-interface,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-grpc-interface
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-tenancy-jooq:
    name: "Gradle · Gradle (jambalaya-tenancy-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-jooq,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-jooq
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-tenancy-jooq:
    name: "Gradle · Gradle (jambalaya-tenancy-jooq)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-jooq,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-jooq
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  github-gradle-jambalaya-tenancy-junit:
    name: "Gradle · Gradle (jambalaya-tenancy-junit)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-junit,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-junit
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  velnor-gradle-jambalaya-tenancy-junit:
    name: "Gradle · Gradle (jambalaya-tenancy-junit)"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',gradle-jambalaya-tenancy-junit,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-gradle.yml
    with:
      unit: gradle-jambalaya-tenancy-junit
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cache_paths: |
        ~/.gradle/caches
        ~/.gradle/wrapper
      cache_key_files: |
        gradle/wrapper/gradle-wrapper.properties
        gradle/libs.versions.toml
  ci-required:
    name: ci-required
    if: ${{ always() }}
    needs: [plan, velnor-lane-admission, policy, github-gradle, velnor-gradle, github-gradle-buildsrc, velnor-gradle-buildsrc, github-gradle-jambalaya-checks, velnor-gradle-jambalaya-checks, github-gradle-jambalaya-checks-jooq, velnor-gradle-jambalaya-checks-jooq, github-gradle-jambalaya-example-grpc-interface, velnor-gradle-jambalaya-example-grpc-interface, github-gradle-jambalaya-example-jooq, velnor-gradle-jambalaya-example-jooq, github-gradle-jambalaya-example-kotlin-test-graphql, velnor-gradle-jambalaya-example-kotlin-test-graphql, github-gradle-jambalaya-example-mapstruct-processors, velnor-gradle-jambalaya-example-mapstruct-processors, github-gradle-jambalaya-graphql, velnor-gradle-jambalaya-graphql, github-gradle-jambalaya-graphql-apollo, velnor-gradle-jambalaya-graphql-apollo, github-gradle-jambalaya-graphql-jooq, velnor-gradle-jambalaya-graphql-jooq, github-gradle-jambalaya-jsr310, velnor-gradle-jambalaya-jsr310, github-gradle-jambalaya-junit-opentelemetry, velnor-gradle-jambalaya-junit-opentelemetry, github-gradle-jambalaya-kotlin-test, velnor-gradle-jambalaya-kotlin-test, github-gradle-jambalaya-mapstruct, velnor-gradle-jambalaya-mapstruct, github-gradle-jambalaya-mapstruct-processor, velnor-gradle-jambalaya-mapstruct-processor, github-gradle-jambalaya-micronaut-graphql, velnor-gradle-jambalaya-micronaut-graphql, github-gradle-jambalaya-micronaut-mapstruct-protobuf, velnor-gradle-jambalaya-micronaut-mapstruct-protobuf, github-gradle-jambalaya-opentelemetry, velnor-gradle-jambalaya-opentelemetry, github-gradle-jambalaya-protobuf, velnor-gradle-jambalaya-protobuf, github-gradle-jambalaya-seo, velnor-gradle-jambalaya-seo, github-gradle-jambalaya-tenancy, velnor-gradle-jambalaya-tenancy, github-gradle-jambalaya-tenancy-flyway, velnor-gradle-jambalaya-tenancy-flyway, github-gradle-jambalaya-tenancy-grpc-interface, velnor-gradle-jambalaya-tenancy-grpc-interface, github-gradle-jambalaya-tenancy-jooq, velnor-gradle-jambalaya-tenancy-jooq, github-gradle-jambalaya-tenancy-junit, velnor-gradle-jambalaya-tenancy-junit]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          FORK_PR: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job policy)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite policy did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job velnor-lane-admission)"
          case "$result" in
            success|skipped) ;;
            *) echo "required CI prerequisite velnor-lane-admission did not pass: $result" >&2; exit 1 ;;
          esac
          selected=",$SELECTED_UNITS,"
          if [[ "$selected" == *",gradle,"* ]]; then
            result="$(result_for_job github-gradle)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle,"* ]]; then
            result="$(result_for_job velnor-gradle)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-buildsrc,"* ]]; then
            result="$(result_for_job github-gradle-buildsrc)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-buildsrc did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-buildsrc)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-buildsrc failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-buildsrc,"* ]]; then
            result="$(result_for_job velnor-gradle-buildsrc)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-buildsrc did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-buildsrc did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-buildsrc)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-buildsrc failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-checks,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-checks)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-checks did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-checks)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-checks failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-checks,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-checks)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-checks did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-checks did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-checks)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-checks failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-checks-jooq,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-checks-jooq)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-checks-jooq did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-checks-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-checks-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-checks-jooq,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-checks-jooq)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-checks-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-checks-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-checks-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-checks-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-grpc-interface,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-example-grpc-interface)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-example-grpc-interface did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-example-grpc-interface)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-example-grpc-interface failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-grpc-interface,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-example-grpc-interface)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-grpc-interface did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-grpc-interface did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-example-grpc-interface)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-example-grpc-interface failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-jooq,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-example-jooq)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-example-jooq did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-example-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-example-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-jooq,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-example-jooq)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-example-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-example-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-kotlin-test-graphql,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-example-kotlin-test-graphql)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-example-kotlin-test-graphql did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-example-kotlin-test-graphql)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-example-kotlin-test-graphql failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-kotlin-test-graphql,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-example-kotlin-test-graphql)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-kotlin-test-graphql did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-kotlin-test-graphql did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-example-kotlin-test-graphql)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-example-kotlin-test-graphql failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-mapstruct-processors,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-example-mapstruct-processors)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-example-mapstruct-processors did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-example-mapstruct-processors)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-example-mapstruct-processors failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-example-mapstruct-processors,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-example-mapstruct-processors)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-mapstruct-processors did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-example-mapstruct-processors did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-example-mapstruct-processors)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-example-mapstruct-processors failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-graphql,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-graphql)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-graphql did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-graphql)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-graphql failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-graphql,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-graphql)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-graphql did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-graphql did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-graphql)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-graphql failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-graphql-apollo,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-graphql-apollo)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-graphql-apollo did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-graphql-apollo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-graphql-apollo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-graphql-apollo,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-graphql-apollo)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-graphql-apollo did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-graphql-apollo did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-graphql-apollo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-graphql-apollo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-graphql-jooq,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-graphql-jooq)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-graphql-jooq did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-graphql-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-graphql-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-graphql-jooq,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-graphql-jooq)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-graphql-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-graphql-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-graphql-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-graphql-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-jsr310,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-jsr310)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-jsr310 did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-jsr310)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-jsr310 failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-jsr310,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-jsr310)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-jsr310 did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-jsr310 did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-jsr310)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-jsr310 failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-junit-opentelemetry,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-junit-opentelemetry)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-junit-opentelemetry did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-junit-opentelemetry)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-junit-opentelemetry failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-junit-opentelemetry,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-junit-opentelemetry)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-junit-opentelemetry did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-junit-opentelemetry did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-junit-opentelemetry)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-junit-opentelemetry failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-kotlin-test,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-kotlin-test)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-kotlin-test did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-kotlin-test)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-kotlin-test failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-kotlin-test,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-kotlin-test)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-kotlin-test did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-kotlin-test did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-kotlin-test)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-kotlin-test failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-mapstruct,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-mapstruct)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-mapstruct did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-mapstruct)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-mapstruct failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-mapstruct,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-mapstruct)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-mapstruct did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-mapstruct did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-mapstruct)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-mapstruct failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-mapstruct-processor,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-mapstruct-processor)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-mapstruct-processor did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-mapstruct-processor)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-mapstruct-processor failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-mapstruct-processor,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-mapstruct-processor)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-mapstruct-processor did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-mapstruct-processor did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-mapstruct-processor)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-mapstruct-processor failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-micronaut-graphql,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-micronaut-graphql)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-micronaut-graphql did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-micronaut-graphql)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-micronaut-graphql failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-micronaut-graphql,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-micronaut-graphql)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-micronaut-graphql did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-micronaut-graphql did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-micronaut-graphql)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-micronaut-graphql failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-micronaut-mapstruct-protobuf,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-micronaut-mapstruct-protobuf)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-micronaut-mapstruct-protobuf did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-micronaut-mapstruct-protobuf)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-micronaut-mapstruct-protobuf failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-micronaut-mapstruct-protobuf,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-micronaut-mapstruct-protobuf)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-micronaut-mapstruct-protobuf did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-micronaut-mapstruct-protobuf did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-micronaut-mapstruct-protobuf)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-micronaut-mapstruct-protobuf failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-opentelemetry,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-opentelemetry)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-opentelemetry did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-opentelemetry)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-opentelemetry failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-opentelemetry,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-opentelemetry)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-opentelemetry did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-opentelemetry did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-opentelemetry)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-opentelemetry failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-protobuf,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-protobuf)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-protobuf did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-protobuf)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-protobuf failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-protobuf,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-protobuf)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-protobuf did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-protobuf did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-protobuf)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-protobuf failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-seo,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-seo)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-seo did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-seo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-seo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-seo,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-seo)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-seo did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-seo did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-seo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-seo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-tenancy)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-tenancy did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-tenancy)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-tenancy failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-tenancy)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-tenancy)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-tenancy failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-flyway,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-tenancy-flyway)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-tenancy-flyway did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-tenancy-flyway)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-tenancy-flyway failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-flyway,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-flyway)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-flyway did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-flyway did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-flyway)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-tenancy-flyway failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-grpc-interface,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-tenancy-grpc-interface)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-tenancy-grpc-interface did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-tenancy-grpc-interface)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-tenancy-grpc-interface failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-grpc-interface,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-grpc-interface)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-grpc-interface did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-grpc-interface did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-grpc-interface)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-tenancy-grpc-interface failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-jooq,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-tenancy-jooq)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-tenancy-jooq did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-tenancy-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-tenancy-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-jooq,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-jooq)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-jooq did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-jooq)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-tenancy-jooq failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-junit,"* ]]; then
            result="$(result_for_job github-gradle-jambalaya-tenancy-junit)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-gradle-jambalaya-tenancy-junit did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-gradle-jambalaya-tenancy-junit)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-gradle-jambalaya-tenancy-junit failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",gradle-jambalaya-tenancy-junit,"* ]]; then
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-junit)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-junit did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-gradle-jambalaya-tenancy-junit did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-gradle-jambalaya-tenancy-junit)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-gradle-jambalaya-tenancy-junit failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ always() }}
    needs: [ci-required]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
